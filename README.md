# Toy sdl3 program to learn myself the basics of zig

```sh
zig build run
```

- `<SPC>` to run the game
- `-` to slow down
- `+` to speed up
- `<ESC>` to close game
- set tiles using left mouse button, automatically pauses on click
